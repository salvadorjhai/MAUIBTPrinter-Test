<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp3.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="10" 
            VerticalOptions="Center" HorizontalOptions="Fill"
            >

            <Picker x:Name="cboPrinters" Title="Select Printers" SelectedIndexChanged="cboPrinters_SelectedIndexChanged" HorizontalOptions="Fill"></Picker>

            <Button x:Name="btnScan" Text="Scan" Clicked="btnScan_Clicked" HorizontalOptions="Fill"></Button>

            <Button x:Name="btnPrint" Text="Print Test" Clicked="btnPrint_Clicked" HorizontalOptions="Fill"></Button>

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
